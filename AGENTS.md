# literacytrends — 엠키(M-key)의 리터러시 동향 위키

이 저장소는 Andrej Karpathy의 "LLM Wiki" 패턴(https://gist.github.com/karpathy/442a6bf555914893e9891c11519de94f)을 따르는, Codex CLI가 관리하는 지속형 지식베이스다. 운영 담당 에이전트 이름은 **엠키(M-key)**. 사용자는 **탤리(Tally)** — 리터러시팩토리(릿팩) 대표 겸 편집장.

**중요 — 이 문서는 자족적이어야 한다.** 엠키는 세션 간에 이 저장소(AGENTS.md + 위키 + 로그) 밖의 기억을 갖지 않는다. 볼트를 관리하는 다른 에이전트(코디, Claude Code 기반)와 메모리를 공유하지 않는다. 알아야 할 모든 맥락은 이 파일과 `wiki/log.md`에 있어야 한다.

이 저장소는 [litfacwiki](https://github.com/youngwoolearning/litfacwiki)에서 discourse 도메인(리터러시 담론읽기)만 분리해 2026-08-17 신설했다. litfacwiki는 language 등 다른 도메인을 계속 담당하는 별도 저장소로 남는다.

## 핵심 아이디어

일반 RAG는 질문할 때마다 원본에서 다시 찾아 답을 조립하므로 축적이 없다. 이 저장소는 대신 **소스를 읽을 때마다 위키(마크다운 페이지)를 계속 갱신**해서 지식이 누적되게 한다.

- **raw/** — 불변 원본(릿팩뉴스레터 "리터러시 담론읽기" 섹션 원고). 절대 수정하지 않는다. 진실의 원천.
- **wiki/** — 엠키가 전담 작성·갱신하는 요약/개념/비교 페이지. 사람은 읽고, 엠키가 쓴다.
- **AGENTS.md**(이 문서) — 구조·컨벤션·워크플로 스키마. 탤리와 엠키가 함께 진화시킨다.

## 저장소 구조 (단일 도메인 — litfacwiki의 domains/ 중첩 없음)

```
literacytrends/
  AGENTS.md          ← 이 문서
  README.md           ← 공개 저장소 방문자용 소개
  LICENSE              ← CC BY-NC-SA 4.0
  raw/                  ← 릿팩뉴스레터 담론읽기 원고
  wiki/
    index.md             ← 내용 카탈로그
    log.md                ← 연대기 로그
    issues/                ← 호별 요약 페이지 (`no-NNN.md`)
    concepts/               ← 호를 가로지르는 주제·개념 페이지
    glossary/                ← 영역별 리터러시 용어집
```

## wiki 페이지 구조

- **`wiki/issues/`** — 뉴스레터 호별 요약. 파일명은 `no-NNN.md`(예: `no-033.md`). 각 페이지에는 원본 호수·주차, 한눈에 보기, 다룬 담론, 중복 점검용 표지어, 관련 기사를 기록한다.
- **`wiki/concepts/`** — 여러 호에서 반복되거나 후속 ingest에서 중복 여부를 대조할 주제·개념. 영문 kebab-case 파일명(예: `ai-judgment-literacy.md`). 근거 호수와 현재 범위를 명시하고, 단일 호에서만 추출했을 때는 반복 주제로 확정하지 않은 초기 개념임을 표시한다.
- **`wiki/glossary/`** — 뉴스레터에서 소개한 영역별 리터러시 용어. 정의·인접 개념·근거 호수를 기록하고, 근거 강도를 `제도·정립`, `반복 관찰`, `초기 관찰`로 구분한다. 현재 통합 용어집은 `new-literacies.md`다.
- 호별 요약과 개념 페이지는 서로 상호 링크하고, 모든 신규 페이지는 `wiki/index.md`에 등록한다.

## 운영 (Ingest / Query / Lint)

- **Ingest**: 새 호 원고가 `raw/`에 들어오면 읽고, 관련 `wiki/` 페이지를 만들거나 갱신하고, `wiki/index.md`를 갱신하고, `wiki/log.md`에 항목을 추가한다.
- **Query**: 탤리가 질문하면 `wiki/index.md`를 먼저 읽어 관련 페이지를 찾고, 필요하면 `raw/`까지 확인해 답한다. 좋은 답은 위키 페이지로 다시 저장해 누적시킨다.
- **Lint**: 주기적으로(탤리 요청 시) 모순되는 서술, 낡은 주장, 고아 페이지, 빠진 상호참조를 점검하고 보고한다.

## git 커밋 — 시도하지 않는다

이 환경의 Codex CLI 샌드박스는 `.git/index.lock` 생성을 차단해 커밋이 항상 실패한다(litfacwiki에서 두 차례 확인됨). **엠키는 git add/commit/push를 시도하지 않는다.** 콘텐츠(raw/wiki 파일) 작업만 완료하면 코디가 검토 후 커밋·푸시한다.

## 공개 저장소 — 주의사항

이 저장소는 public이다. wiki·raw 파일에 내부 논의용 표현(코디·판다 등 다른 에이전트 이름, 릿팩 내부 의사결정 과정 등)이 섞이지 않도록 주의한다. raw 원고 자체가 이미 릿팩뉴스레터로 공개 발행된 내용이라 원칙적으로 문제없지만, wiki 페이지를 새로 쓸 때도 같은 기준을 유지한다.

## log.md 형식

```
## [YYYY-MM-DD] ingest | 소스 제목
## [YYYY-MM-DD] query | 질문 요약
## [YYYY-MM-DD] lint | 점검 범위
```

## index.md 형식

`wiki/index.md`는 내용 카탈로그다. 카테고리별로 페이지를 나열하고, 각 항목은 링크 + 한 줄 요약 + (있으면) 날짜/출처 개수 메타데이터를 포함한다.

## 세션 시작 절차

탤리가 "업무 시작"(또는 유사 표현)으로 대화를 열면:

1. `date`로 현재 시각 확인
2. `wiki/log.md` 최근 항목 확인
3. `raw/`에 아직 ingest 안 된 새 원고가 있는지 확인
